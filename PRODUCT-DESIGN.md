# 产品功能与流程设计 PRODUCT-DESIGN（以 WATCH-METHODOLOGY 为蓝图）

> 把 `WATCH-METHODOLOGY.md` 的盯盘方法论落成 **frontend-dsh（@lisonevf/dsh-stock-panel）** 的功能与流程设计。
> 范围：**蓝图 + 核心逻辑骨架（数据结构与判定要点）**，不含实现代码。定位：**盘中/盘后的执行台**，不是全功能量化平台。
>
> **阅读指引（2026-09-12 校订）**
>
> | 章节 | 状态 |
> | --- | --- |
> | **§6e 信息架构 A6：导航平铺** | ✅ **唯一现行设计**（板块与复盘/作战同级；下线自选盘/个股明细/看盘三个入口；抽屉机制整体移除） |
> | §6c / §6d | 🟡 部分被 §6e 取代：§6c 的四层关注点与流程入口、§6d 的左栏自选+个股 / 主区个股信息仍有效；**导航形态以 §6e 为准**（§6d.2 第 3 条的「工具单页」已下线） |
> | §2.3 数据源约束 / §4 / §5 | ✅ 有效（§4.3、§5.1–§5.4 的骨架已实现，作契约参考）；§7 数字为 2026-09 设计期预算，实测口径见 `docs/ARCHITECTURE.md` §3.1 |
> | §1 原则 5 / §2.1–§2.2 / §3.1 / §3.3 / §6 / §6b / §8 | ⚠️ **历史与已废方案** → 见「历史与已废方案」小节 |
>
> 现行总览：`README.md` · 架构：`docs/ARCHITECTURE.md` · 待做：`docs/ROADMAP.md`；旧实施计划（M/N/W 批次）已归档 `docs/archive/`。

## 1. 设计原则（先立规矩，防范围膨胀）

1. **一个目标**：帮助用户每天回答方法论里的三个问题（Q1 持续性增减？Q2 有无局势变化？Q3 动作？），其余功能不为此服务的一律不做。
2. **降噪优先**：页面只展示「相对昨日/上一轮的**变化**」；列表全设上限（≤20 行）；无意义的实时跳动不渲染。
3. **复用优先**：梯队/涨停池/板块热度/全 A 快照等 M6/M5 资产直接复用，不新造数据管道。
4. **时段感知**：交易日时钟（`server_info`）驱动视图自动切换：竞价(9:15-9:25)/盘中/复盘(15:10 后) 呈现对应内容，减少手动切换。
5. ~~**纯 MCP、无后端**~~：⚠️ **已过时**——行情确已纯内置（embedded node-tdx，见 `README.md`），但「关键价位」等仍有 HTTP-only 残留（静默失效，见 `docs/ROADMAP.md` B6）。
6. **宽视图优先**：视图占满会话列（官方 `conversation.view` 标签页形态），新页面按「主列表 + 右侧详情」的自适应布局设计；窄屏（<720px）退化为单列堆叠，不设固定列宽。

**明确不做**（防噪音与范围膨胀）：不扩展 AI 分析；不引入自动下单/推送交易信号；不新增图表库；不做全市场分钟级扫描；不做「当日所有涨停」之外的历史复盘回溯（仅存近 N 日快照）。

## 2. 现状盘点与能力差距

> ⚠️ **原 §2.1（现有资产 M5/M6）/ §2.2（能力差距）已归档**：条目均已过时或被实现取代，现状以 `README.md` 与 `docs/ARCHITECTURE.md` 为准。

### 2.3 数据源约束（实测确认，设计必须遵守）

| 约束 | 影响 | 对策 |
| --- | --- | --- |
| `unusual/market_monitor` 收盘后仅剩 09:15 竞价快照，缺炸板类事件 | 盘中事件只能在盘中捕获 | `event-stream.ts` 盘中轮询并**增量落盘**（localStorage JSONL），收盘后显示已捕获的历史 |
| `auction` 返回当日 09:15–09:24:57 逐点 `matched/unmatched`（盘后可查当日竞价） | 竞价分析只能在当日做；次日需在 9:15 后轮询 | 复盘时把「当日竞价」纳入盘面回顾；次日竞价对照在 9:15+ 现场取数。⚠️ `unmatched` 正负号语义（买单/卖单未匹配方向）未定论，实现前须协议冒烟实测（§7.1 纪律：禁止凭 schema 猜） |
| `board_members` 的 `sort_type=AMOUNT` 报错 | 按金额需客户端自排 | 取数后本地排序；板块脉冲拉全量成分后自算 |
| 单请求可能 15s 超时 / 全 A 响应 ≥2MB | 批量请求需并发限制与容错 | 沿用 `pool.ts`(≤6 并发) + mcp 15s 超时 + 失败降级 |
| `symbol_info.vr`（量比）与 `quote` 同源 | 竞价强度需用「竞价量/昨日同期」等自算量 | 竞价特征以 `auction.matched` 时间序列为主，不依赖外部量比字段 |

## 3. 信息架构与流程总览

> ⚠️ **§3.1 的导航形态已作废**（写于「6 Tab」时代）：现行导航 = **流程两段（复盘/作战）+ 板块平铺**（行情/自挖板块/选股筛选/监控规则/外盘），见 §6e。**§3.2 的三条数据管道划分（P1 复盘 / P2 竞价 / P3 盘中）仍然有效**，实现落在 `lib/review-*`、`lib/auction-analysis`、`lib/event-stream`+`lib/board-pulse`。
> 原 §3.3（与现有页面的联动：点行开「个股」Tab）已归档 / 已由 §6e 取代：点任意一行 = 设标的 + 切到工作台；自选徽标默认关闭的降噪默认不变。

### 3.1 导航（新增 1 个 Tab，共 6 个，可横滚）

> （原 §3.1 已归档 / 已由 §6e 取代：现行入口 = 复盘 / 作战 / 行情 / 自挖板块 / 选股筛选 / 监控规则 / 外盘；工作台不上导航栏。）

### 3.2 三大数据管道（页面背后）

| 管道 | 触发 | 步骤（模块与参数） |
| --- | --- | --- |
| **P1 复盘** | 15:10+ 触发 / 手动 | `fetchAllA`(缓存) + `loadLadder`(涨停池/梯队/板块热度) → 汇总 涨停/跌停/最高板/晋级率/炸板统计/首板溢价(需昨日池) → `strength.ts` 对候选集(≤120) 拉 `kline(DAILY,5)` 算 3 日相对强度差分 → `regime.ts` 温度计刻度 → `review-store.ts` 写 `ReviewSnapshot`(今日) + 默认生成次日预期草稿(龙头/主线/失败条件) |
| **P2 竞价** | 9:15–9:25，会话内 | 观察池 = 昨日预期清单 + 今日涨停候选 + 自选(上限 ~40) → `auction-analysis.ts` 每标的 `auction(市场,代码)` → 竞价强度特征 → 与 `ReviewSnapshot.预期清单` 对照 → 强弱转换标记(弱转强/强转弱/证伪/超预期) → 陷阱规则(`isFalseStrong`) 过滤后进「竞价雷达」列表(≤15) |
| **P3 盘中** | 9:30–15:00，30s 轮询，页面隐藏暂停 | `event-stream.ts` 捕获 `unusual`×3市场 + `market_monitor`×3 增量(去重落盘)；`board-pulse.ts` 以热度板集合(≤8, 取当日 `ladder.top`) × `board_members(count≤100)` 每轮差分 → 涨停数/涨速/成交占比变化 > 阈值 → 脉冲事件；`regime.ts` 温度计按最新广度/梯队滚动 → 局势归类(`strength`/`regime` 规则表 §5.3) → 决策条 Q1-Q3 |

## 4. 复盘模式（P1 管道）设计

### 4.1 页面块（自上而下）

| # | 块 | 要点 |
| --- | --- | --- |
| 1 | **今日盘眼** | 指数/广度/成交额一行 + 温度计刻度 + 主线一句话（系统给出候选，人确认） |
| 2 | **自动回顾区** | 涨停梯队条形（复用 `LadderPage` 逻辑，只读精简版）+ 跌停/大面统计；炸板/大面清单（昨日涨停今日 <−5% 或曾涨停后开板；≤10 行，标时空标签）；板块热度 TOP（复用 `ladder.boards`）+ 每板块「时空标签」建议（题材第几天由用户点选或默认 1） |
| 3 | **预期清单编辑器** | 卡片式，字段固定（WATCH-METHODOLOGY §3.3 的 7 字段），上限 5 张；支持一键「从今日涨停池加入龙头」 |
| 4 | **存档** | 保存为当日 `ReviewSnapshot`（JSON），追加历史（默认保留 60 个交易日） |

### 4.2 数据结构骨架（`lib/review-store.ts`，localStorage key `dsh-stock-panel:review:v2`）

- `PhaseTag`：`day1`/`day2+`（题材生命周期）、`low`/`mid`/`high`（空间位置）、`leader`/`follower`/`catchup`（梯队角色）；`ExpectItem`：`id`、`symbol`(`'SH603138'`)、`name`、`tags{themeDay?, level, role}`、`state`(`strong`/`divergence`/`weak2strong`/`highRisk`/`recession`/`newLow`)、`scenario`(明日剧本，≤40 字)、`auctionOK`(竞价条件，可量化，如 "高开3%+竞价量>昨日20%")、`failIf`(失败条件)、`reason`(一句话理由，必须含量价证据)。
- `ReviewSnapshot`：`day`(`'2026-09-04'`)、`savedAt`、`breadth{up,down,limitUp,limitDown,amountYi}`、`regime`、`mainLine[{board, leader}]`、`expectations`(≤5)、`notable[]`(关键事件时间线，压缩)。

### 4.3 相对强度差分（真强 vs 惯性）

输入 ≤120 只候选（昨日涨停池 + 今日涨停池 + 自选 + 板块代表），对每只拉 `kline(DAILY,5)`：`pct_today`、`cum5`（5 日累计涨幅，空间）、`delta3`（3 日动量差分 = `(pct_t-2+pct_t-1+pct_t) − (pct_t-5..pct_t-3)`）、`volRatio`（当日量/5 日均量）、`atLimit`、`streak`。

| 判据（`classifyStrength`，WATCH-METHODOLOGY §4.2） | `StrongKind` |
| --- | --- |
| `atLimit && volRatio >= 1.2 && delta3 >= 0` | `trueStrong` |
| `atLimit && delta3 < 0`（★ 假强：惯性封板） | `inertia` |
| `!atLimit && cum5 > 15 && volRatio >= 2 && pct_today < 0`（高位放量滞涨） | `weakening` |
| `!atLimit && delta3 > 0 && volRatio >= 1.5` | `weak2strong` |
| 其余 | `flat` |

## 5. 盘中模式（P2/P3 管道）设计

### 5.1 竞价雷达（9:15–9:35 高亮）

`lib/auction-analysis.ts` 输入 `auction` 返回 `items: {time,price,matched,unmatched}[]`；`AuctionFeatures` 字段：`openPct`（竞价末端价 vs 昨收，即预期开盘幅）· `nearLimit`（末端价贴近涨停价）· `slope920_925`（9:20→9:25 `matched` 累积斜率，真实意图段）· `climaxDir`（9:24:30-9:25:00 冲刺方向，price/matched/unmatched 共同判定：`buy`/`sell`/`none`）· `matchedTotal`（竞价总匹配量，可对比昨日同期/个股历史分位）· `fakeBigThenDrop`（9:15-9:20 大量挂单后撤单跳水，诱多；判据：9:15-9:20 `matched` 峰 − 9:20 后回落幅度 > 40%）。

⚠️ 实现前先冒烟验证 `unmatched` 正负语义与点位抽样（开盘后不再更新？收盘后可回看？），并确认 `matched` 单位（手/股）与涨停价判定（`price≈buy_price_limit` 需 `pre_close×1.1/1.2` 对齐）。

**对照矩阵（预期 × 竞价）→ `Verdict`**：`beatExpect` / `confirm` / `falsify` / `weak2strong` / `weak2weak` / `trap`（与 WATCH-METHODOLOGY §4.4 同构）。

| 判据（`judgeExpectation`） | 结果 |
| --- | --- |
| `exp==='strong' && openPct >= 3 && 板块 ok`（`boardOk = !boardPulse \|\| boardPulse.limitUp >= 1`） | `beatExpect` |
| `exp==='strong' && (openPct < 0 \|\| climaxDir==='sell')` | `falsify` |
| `exp==='divergence' && slope920_925 > 0 && climaxDir==='buy'` | `weak2strong` |
| `nearLimit && exp==='highRisk'`（高位竞价高开 → 陷阱候选） | `trap` |
| 其余 | `confirm` |

- 陷阱二次过滤 `isFalseStrong`（WATCH-METHODOLOGY §4.3 三件套）：高位(≥4 板) + 竞价高开 + (板块涨停数环比降 \| 无龙头共振) + 开盘 10min 无承接 → 标 `trap`。
- **竞价雷达列表（≤15 行）**：每行 = 名称/代码 + 预期状态图标 + 竞价特征迷你（高开幅、冲刺方向、量）+ Verdict 徽标（超预期红/证伪绿/陷阱橙）。**只突出与昨日预期不一致者**，一致的整行置灰（降噪）。

### 5.2 情绪温度计

`lib/regime.ts` —— 输入：全 A 快照 + 涨停池统计 + 昨日首板池。`RegimeInputs` 字段：`limitUp`/`limitDown`/`maxStreak`（涨停数/跌停数/最高连板）· `promoteRate`（晋级率 = 昨日涨停今日仍涨停/昨日涨停数）· `brokenRate`（炸板率 = 炸板数/曾涨停数）· `firstBoardPremium`（首板次日平均溢价 %，需昨日首板池）· `upRatio`（上涨家数占比 0-1）· `amountYi`（两市成交额(亿)，分位化用）。

- `Regime.band` = `ice`/`cold`/`warm`/`hot`/`overheat`；`computeRegime` 各输入先做阈值分段打分(0-100 加权)，加权 = 温度 `temperature`。
- 特别规则：`promoteRate < 0.25` 或 `brokenRate > 0.5` ⇒ 温度强制 ≤ 45（退潮/分歧）；`maxStreak >= 5 && 缩量加速(upRatio 高但 amount 降)` ⇒ 触发 `overheat`。
- `drivers` 记录拉高/压低项（≤3），供人工复核而非只看一个数字；联动：**仓位总闸** = `band → G(总仓上限)`（WATCH-METHODOLOGY §6.2 表格固化进 `sizing.ts` 默认参数）。

### 5.3 事件流与局势归类

- `lib/event-stream.ts`（增量捕获）：轮询 `unusual`×3 + `market_monitor`×3（30s）；key `${market}-${code}-${time}-${desc}` 去重；写 localStorage(JSONL 环形 ≤500 条)；重启后补齐；收盘后只读（显示盘中已捕获历史，标注「收盘后 unusual 退化为竞价快照」）。`EventItem`：`ts`、`market`(SH/SZ/BJ)、`code`、`name`、`desc`、`kind`(`limitUp`/`limitDown`/`break`/`surge`/`bigOrder`/`other`)、`time`、`value`。
- `lib/board-pulse.ts`（板块资金脉冲）：热度板集合(≤8) 每轮 `board_members(count≤100, CHANGE_PCT desc)`；板块涨停数 δ、板块内均涨幅 δ、成交额占比 δ > 阈值（默认 涨停 +1 或 均涨幅 > +1.5pp）→ `pulse` 事件；`BoardPulse` = `board`、`name`、`limitUp`、`avgPct`、`amountShare`（与上轮差分后仅保留 δ 显著者）、`leader{code,name,pct}`。
- 局势归类（把观察翻译成局面 → 决策条 Q1-Q3）：`Situation` = `highLowSwitch`（高位龙头炸板/断板 + 低位批量 `limitUp` + 指数平稳）/ `innerDivergence`（主线龙头分歧但板块 `limitUp` 仍增）/ `newDirection`（陌生板块集体 `surge` ≥3 只 + 无昨日主线共振）/ `weightLift`（指数↑但 `limitUp` 少，广度差）/ `recession`（`limitDown` 增多 + `promoteRate` 骤降 + 炸板率升）/ `normal`。

### 5.4 我的持仓与决策条

`lib/positions.ts` + `lib/sizing.ts`。`Position`：`symbol`、`name`、`entryDate`、`entryPrice`、`shares`、`plan{stopLoss, failIf, target?, note}`（失败条件复盘时已写）、`expectState`（关联当日预期）。

决策条三行 —— **Q1 持续性**：持仓对象 `regime.band` + `classifyStrength`(实时快照) → 增强/衰减/中性；**Q2 局势**：`judgeSituation(...)` 结果 → 提示文本（如「高低切进行中：老龙头兑现，低位新方向观察」）；**Q3 动作**：持仓触发 `failIf`/断板/破位 → 卖，竞价 `Verdict=weak2strong` 且板块 ok → 可半仓试错，否则观望。

**仓位（`kellyPosition`，WATCH-METHODOLOGY §7，参数集中可配）**：`KellyArgs` = `p`/`b`（持有期口径自统计）、`fraction`（默认 0.5 即 1/2 凯利 → 0.25）、`gateCap`（温度计总仓闸门 ×账户）、`stopLossPct`（计划止损幅 %）。计算：`fStar = b·p − (1−p)/b`（若 `p*b ≤ q` → 0），`fStar <= 0` 返回 0；`byKelly = fStar × fraction × 100`；`byStop = gateCap × (stopLossPct / 10)`（示例换算：名义仓位 = 风险预算/止损幅）；返回 `Math.min(byKelly, byStop, 10)` —— **硬封顶单票 ≤10%**。附加护栏（常量）：单板块合计 ≤30%；同日独立逻辑 ≤4；两跌停最坏情景 ≤账户 8-10%。

**自统计入口**：每笔平仓后记录（信号类型/结果/持有期）→ `trade-log`，复盘页提供 p̂/b̂ 滚动统计（**≥50 样本才启用建议仓位**，样本不足显示「无证据，空仓」）。

## 6b. 页面骨架（宽视图 / v1.3 沉浸式，取代 §6 的窄列骨架）

> （原 §6 已归档：窄列 ASCII 骨架作废；原 §6b 已归档：4 视图 + 1-4 快捷键已被 §6c 取代，其「三段式」原始定义 —— 状态带 / 左栏列表·主图 / 右栏 AI —— 仍是现行骨架来源。现行排版见 §6e。）

### 历史与已废方案（仅溯源，勿当现状）

| 曾经的设计 | 被什么取代 | 现在去哪看 |
| --- | --- | --- |
| §2.1/§2.2（M5/M6 时代资产与能力差距盘点） | 资产已复用、差距多已实现或重估 | `README.md`、`docs/ARCHITECTURE.md` |
| §3.1 导航「6 Tab」（市场/梯队/指数/自选/个股/作战）、§3.3 点行开「个股」Tab | §6c/§6d → **§6e 导航平铺** | §6e.2 / §6e 决策 3 |
| §6 ASCII 窄列骨架 / §6b 4 视图 + P1–P3 批次 / §6c.4 工具抽屉 / §6c.5 旧习惯兼容清单 | §6c（流程入口）→ §6d → §6e（抽屉机制整体下线，左栏即入口） | §6c.2/§6c.3 的四层与时段表；§6e 决策 2/5 |
| §6d.2 第 3 条「工具单页」（外盘弱化为页内区块） | §6e：板块与复盘/作战平铺 | §6e.2 |
| §8 N1–N7 批次表（已全部交付） | 现行路线图两轨（A 产品闭环 / B 工程地基） | `docs/ROADMAP.md`、`CHANGELOG.md` |
| §1 原则 5「纯 MCP 无后端」 | 已被 B 轨残留推翻 | `docs/ROADMAP.md` B6 |

## 6c. 统一看盘关注点与流程 → 排版重规划（v1.4）

### 6c.1 结论：把「功能分块」换成「流程分块」

v1.3 的 4 个功能视图**功能齐全但流程断裂**，而 `WATCH-METHODOLOGY.md` §8 给的是**固定时间表**：

> 15:10–16:00 复盘写清单 → 9:15–9:35 竞价对照 → 9:30–15:00 只答 Q1–Q3 → 14:30 尾盘决策

所以排版的第一原则是**时段驱动**（§3.1 原始蓝图即此设计），功能块下沉为「工具」（工具层本身已被 §6e 拿掉）。

### 6c.2 统一的四层关注点（任何时段都只看这四层）

| 层 | 看什么 | 数据/模块 | 它决定什么 |
| --- | --- | --- | --- |
| **① 环境** | 指数方向、涨跌家数、涨停/跌停、成交额分位、情绪温度分档 | `market.ts` `regime.ts` | **总仓位闸门 G**（温度计是总闸） |
| **② 主线** | 板块涨停数（结构而非计数）、最高连板、梯队是否断层、主线内龙头 | `ladder.ts` `board-pulse.ts` | **只对主线给仓位**；支线只看 |
| **③ 标的** | 我的预期清单对象、持仓、总龙头、炸板/大面样本 | `review-store.ts` `positions.ts` `event-stream.ts` | 盯谁、谁的失败条件触发了 |
| **④ 动作** | 清单对照判定 / Q1–Q3 / 兑现换股 / 仓位 | `mission.ts` `auction-analysis.ts` `sizing.ts` | **今天做什么**（唯一输出） |

**"杂乱"的判定标准**：任何一块信息，如果不能明确归到上面四层之一，就不该出现在主区（可进次级入口）；这条规则可直接用于下一轮功能清理。

### 6c.3 五个时段 × 四层的具体形态

| 时段 | ① 环境 | ② 主线 | ③ 标的 | ④ 动作（唯一输出） |
| --- | --- | --- | --- | --- |
| **复盘** 15:10–9:15 | 温度计 + 广度 + 成交额分位（七步①） | 连板梯队 + 板块结构 + 资金流向（七步②③④） | 昨日涨停今日表现 / 大面样本 / 风向标（①b⑤⑥） | **次日预期清单 ≤5 条（7 字段）+ 存档**（七步⑦） |
| **竞价** 9:15–9:30 | 指数竞价方向 | 主线板块是否同步 | **预期清单对象** × 竞价特征 | 每条预期的判定：超预期/证伪/弱转强/陷阱 |
| **验证窗** 9:30–10:00 | — | 主线扩散度 | 竞价判定对象 | 高开承接 / 低开收复 / 弱转强放量 → 半仓试错 |
| **盘中** 10:00–14:30 | 温度滚动 | 板块脉冲 + 涨停比 | 事件流 + 持仓 + 龙头状态 | **Q1 持续性 / Q2 局势 / Q3 动作** |
| **尾盘** 14:30–15:00 | — | 主线是否留强 | 持仓 + 换股候选 | 兑现 / 换股 / 定仓（T+1 唯一动作点） |

### 6c.4 落地的排版

> ⚠️ **导航形态以 §6e 为准**（原「一级导航 3 项 + 工具抽屉」已被 §6e 导航平铺取代）。仍然有效的排版纪律：

- **阶段头**：把方法论原文的「本阶段必须回答的问题」摊在页面顶部 —— 页面替用户记住流程；
- **列流排版**（`.dc-flow`）：宽屏 2–3 列紧密接排，窄屏自动单列；复盘页把七步的**信息步（①–⑥）并排**，而**⑦ 输出（预期清单）独占整行** —— 信息并排看、结论单独写；
- **自动跟随 + 手动优先**：跨过 9:15 / 14:30 / 15:10 边界时自动切到对应入口；用户手动点过导航后本次会话不再自动跟随（不打断正在做的事）。

## 6d. 信息架构 v1.5（2026-09-12 决策记录）

> 本节是**已拍板的决策**，取代 §6b 的 4 视图与 §6c.4 的抽屉细节；实施项见 `docs/ROADMAP.md` A1–A5。

### 6d.1 定位：盯盘执行台（不是全功能量化台）

| 能力 | 归属 |
| --- | --- |
| 盘中/盘后执行（复盘清单 → 竞价对照 → Q1–Q3 → 尾盘定仓）、盯盘台、AI 研判 | **本插件** |
| 自挖概念**发现**（HIST 共动类） | **本插件内置**（node-tdx HistEngine） |
| 自挖概念**命名**（LLM 归纳 + 护栏） | **本插件 host 半移植**自 `cluster-namer`（口径来源，算法上游仍是它） |
| 选股引擎（20 策略）、回测（vectorbt）、财务、数据管道/外部数据接入；自挖类算法研究（co_cluster daily/api）、情绪/簇命名其他试验 | 父项目 `tickflow-stock-panel`（`frontend/` SPA + `backend/` FastAPI）与兄弟项目 `kronoros` / `sentiment` / `cluster-namer` 各自演进，**均不在本插件范围** |

**边界规则**：任何新需求先问「它服务的是『今天该做什么』吗」——否，则不做或向上游项目提。

### 6d.2 布局（三段式保持不变，栏的内容变）

三段式：状态带 ｜ 左栏（自选 / 个股）｜ 主区（**选中标的即渲染个股信息**：报价头 + 图区日K⇄分时 + 概念区官方板块与自挖共动类 + 下部资金流/逐笔/竞价）｜ 右栏 AI 研判（一键研判 → 决策卡、关键价位画到图、深入对话）。

> ⚠️ 状态带里的 `[复盘][作战][看盘]│[工具]` 已被 **§6e（A6 导航平铺）** 取代：现在是 `[复盘][作战][行情][自挖板块][选股筛选][监控规则][外盘]`，工作台（原「看盘」）不上导航栏。

三条落地要求：① **左栏保留自选栏**，点行即渲染个股信息（不再是「跳页」）；② **左栏新增「个股」分组 = 最近看过的个股**（按浏览顺序，上限可配，落盘持久化），点行同样渲染个股信息，**下线「涨停」「异动」两个分组**（涨停已有「涨停梯队」，异动并入对应入口）；③ **工具合并为一张单页**（⚠️ 已作废，见 §6e：板块改为与复盘/作战平铺）：市场总览 / 指数 / 涨停梯队 / 选股筛选 / 自选盘 / 监控规则 / 个股明细 / 外盘 在同一页分区呈现（⌘K 直达区块），**外盘弱化**为页内次要区块（默认折叠，不再占独立入口）。

### 6d.3 特色板块：自挖概念 + 命名（本插件差异化能力）

- **发现**（已内置）：HIST 残差共动 → 无监督聚类 → 当日「市场自己认定的班」；
- **命名**（v1.5）：成员涨停/异动素材 + LLM 归纳 + 护栏 → `named / no_common / insufficient`，并给出与官方花名册的关系（`官方已有（收敛）` / `叙事漂移` / `官方盲区`）；
- **口径硬约束**（沿用 cluster-namer）：素材**只用于解释、不进聚类输入**；证据**必须可反查**（引文比对 + 时间窗 + 覆盖度门控）；**拒绝命名是一等公民**；
- **前置门槛**：先做参数校准（pool_n/window/min_corr）+ 弱链过滤 + 跨日稳定性，达标后才上 UI；**呈现**：主区「概念区」为主（个股视角 + 当日类列表），复盘②主线识别引用为辅。

#### 6d.3b 「自挖板块」页的呈现决策（2026-09-12，用户拍板）

类列表从"次要区块"升为一级入口后，用户提出三条硬要求，落地方案如下（实施见 `CHANGELOG` A6 补强）：

| 要求 | 决策 | 理由 |
| --- | --- | --- |
| **模型命名归类**（打开就有名字） | 命名改**批量**：一次并集采集 + 按成员数分批的少量调用（多组一次问完），逐组独立过护栏 | 逐类点 = 十次请求，且模型看不到组间区别（会把"铜"和"铝"都叫成"有色金属"）；批量的批次边界只影响请求次数，**不放松任何一组的护栏**（跨组借证据会被拒） |
| **按强度排序 + 要涨幅和涨停数** | 强度 = **均涨幅% + 6×涨停数**；并列看强边密度 → 类内相关 → 规模。每行给 涨停数 / 均涨幅 / 最大涨幅 / 上涨只数 + 成员票（可点开看图） | 盯盘只关心"哪个班猛"：整体在涨 + 有没有封板；权重是显式常数（`STRENGTH_LIMIT_UP_WEIGHT`），改它必须同时改注释与单测。涨幅与涨停数是强度分的分量，**必须与排序同源**（口径唯一来源 `lib/concept-strength.ts`）；涨停用 `buy_price_limit` 精确判定（各板幅度不同，阈值法必错；无涨跌幅限制的新股不计入） |
| 预算纪律 | 页头显示「本次模型调用 N 次 · 缓存命中 M 组」；重复打开复用整批 memo（不采集、不调模型） | 自动命名是"打开就跑"，因此**每一分预算都必须可见**；`hist_concept_classes` 的强度聚合与引擎共用 60s 全A快照 memo，不重复拉 ≥2MB |

### 6d.4 复盘校准页（必要）

方法论写的「与人工判定一致率 ≥80%」从未被测量过；数据其实已经在手：`dayrun` 记录了 Q2 的 `from: 'auto' | 'manual'`、逐条竞价判定 `verdicts`、Q1/Q3 与持仓动作。校准页要做的是把这些**反算成可回看的分数**：① 系统候选 vs 人工确认（局势/竞价判定一致率、分歧样本清单）；② 预测侧：昨日预期清单 × 今日实际结果（兑现/证伪命中率，已有 `review-metrics` 基础）；③ 阈值校准：温度计分档、强度分类、共振阈值的「按自身历史校准」入口；④ **只呈现统计与分歧样本，不自动改用户结论**（沿用「AI 不覆盖草稿」立场）。

### 6d.5 持久化（host 侧）

全部本地资产从 localStorage 迁到 DSH 存储子系统的 `stock-panel` 领域（`ctx.storageDomain` + `defineDomain`，json 后端落 `$DSH_HOME/storages`），**localStorage 降级兜底**；含**个股栏历史**。设计与迁移策略见 `docs/ARCHITECTURE.md` §5.2。

## 7. 轮询与性能预算

> ⚠️ 本节数字为 2026-09 设计期预算。**实测口径（含 ladder 占 ~90% 请求、栏收起不停轮询等债务）已迁到 `docs/ARCHITECTURE.md` §3.1**，对策见 `docs/ROADMAP.md` B3。

| 数据 | 频率 | 量 | 说明 |
| --- | --- | --- | --- |
| server_info | 60s | 1 请求 | 时段感知；只在作战页激活时 |
| unusual+market_monitor | 30s ×6 市场参数 | ≤6 | 事件增量；作战页隐藏即暂停 |
| 全 A 快照 | 20s TTL 缓存 | 1（≥2MB） | 复盘与盘中广度复用，不重复拉 |
| 热度板 members | 30s ×≤8 板 | ≤8 | count≤100/板；仅在 P3 激活时 |
| auction | 9:15–9:25 每 10s ×≤40 标的 | 240/时段 | 仅在竞价时段轮询；开盘后停 |
| kline(DAILY,5) 强度差分 | 复盘 1 次 ×≤120 | 120（pool≤6） | 约 20–40s，带进度 |
| 历史存储 | localStorage | JSONL ≤500 事件、复盘 ≤60 日 | 超限环形裁剪 |

> 合计峰值 ≈ 全 A + 8 板块 + 竞价批量，均在既有并发护栏内；非激活 Tab 一律暂停轮询。

## 6e. 信息架构 A6：导航平铺（现行）

> 本节是**已拍板的决策**，取代 §6d.2 第 3 条的「工具单页」与 §6c.4 的抽屉细节。一句话：**把「工具」这一层拿掉**。

### 6e.1 为什么（A4 的抽屉是过渡形态）

A4 把 8 个功能收进「工具」单页，理由是"不占一级导航"：一级导航只回答「现在该干什么」。用下来暴露两个问题：

1. **每个板块本来就与流程同级**：行情 / 自挖板块 / 选股筛选 / 监控规则 / 外盘各自独立取数、各自有轮询节拍，抽屉只是多了一次"点工具 → 再找区块"的跳转；
2. **同一件事出现了两个入口**：自选既是左栏分组、又是「自选盘」整页；个股既是左栏行、又是「个股明细」整页；而「看盘」与工作台也是一页两名。

### 6e.2 决策

```
┌ 状态带：阶段(可点)│[复盘][作战][行情][自挖板块][选股筛选][监控规则][外盘]│指数条│广度│情绪≈│[问模型][⌘K] ┐
```

| # | 决策 | 理由 |
| --- | --- | --- |
| 1 | **导航平铺**：旧抽屉里的板块与复盘/作战同级 | 一次点击直达；`1-7` 键位即页面顺序 |
| 2 | **摘除「自选盘」「个股明细」** | 左栏常驻列表**就是**它们的入口（点行即看）；不再各留一个整页 |
| 3 | **摘除「看盘」入口** | 它不是流程的一环，而是"点左栏任一行 / 任意页面点股票"落到的主区 —— 改称**工作台**，不占导航栏、不占键位，因此也永远不会"切走回不来"（左栏随时点一下就回来了） |
| 4 | **外盘仍是次要** | 排最后 + 颜色更淡（弱化不等于藏起来） |
| 5 | **抽屉机制整体下线** | `ToolHost` / `TOOL_VIEWS` / `ui.tool` / `t` 键 / Esc 的 "tool" 层全部删除：没有抽屉，就没有"抽屉开着时其余键让路"这类规则 |
| 6 | **默认入口 = 行情**（`DEFAULT_VIEW`） | v1.4 的默认是"按时段落到复盘/作战"，实测不好用：行情（总览 + 指数 + 涨停梯队）是**任何时段都成立的第一眼**，不必先猜"现在几点"。复盘/作战一键可达（`1`/`2`），**时段边界仍会自动跟随**（用户没手动点过导航时） |

### 6e.3 硬约束（不许在下一轮被做没）

1. **一次只挂载一个入口**（`views/ViewHost.tsx` 的 `switch`，不是一排 `display:none` 容器）：`display:none` 不停轮询，只有卸载才停 —— 涨停梯队单轮 ≤177 次工具调用，这条不能破；
2. **工作台必须在 `ALL_VIEWS` 里、且不在 `PRIMARY_VIEWS` 里**：前者保证「点股票」不会落到非法视图（`viewIdOf` 会拒绝），后者保证 `1-7` 键位与状态带不会多出一项；
3. **落盘值必须继续可迁移**：`viewIdOf()` 把旧 id（`overview`/`indices`/`ladder` → `market`，以及当年 `tool` 字段里的同名值）就近映射；已经下线的 `watchlist` / `detail` 一律拒绝（拒绝 = 回默认入口，不是"打不开"）；
4. **默认入口必须是导航栏上的入口**（`DEFAULT_VIEW ∈ PRIMARY_VIEWS`）：落到工作台或未知值都会让首屏**没有任何一项高亮**（用户不知道自己在哪）—— 单测钉住这一条。
5. **默认入口要真的看得见**：只改 fallback 不够 —— 老用户浏览器里的 `ui:v3`（上次停在复盘/作战）会压过新默认。因此 UI 键升到 `stock-panel:ui:v4`，且**读到旧键时一次性落到默认入口**（`initialViewOf(persisted, fromLegacy)`，其余偏好照常继承）；写回 v4 之后不再重置，"视图跨卸载保持"的既有要求不受影响。

## 8. 建议实施批次（历史：N1–N7 已全部交付）

> ⚠️ **历史章节**：N1–N7（`session-clock`+`review-store`+复盘模式页 / `auction-analysis`+竞价雷达 / `strength` 强度差分+低位首板候选 / `event-stream`+`board-pulse`+盘中模式页 / `regime` 温度计+总仓闸门 / `positions`+`sizing`+交易日志自统计 / 个股页补竞价回顾与资金面板）已全部交付（见 `CHANGELOG.md`）。**现行路线图在 `docs/ROADMAP.md`**（两轨：产品闭环 A1–A5 / 工程地基 B1–B6），本节批次划分仅作溯源。

**验收锚点（每个页面独立可测）**：复盘——保存后刷新不丢，次日竞价页能读出昨日 `expectations`；竞价——9:25 后 15s 内出全部观察池特征，与人工判定一致率 ≥80%（前 20 交易日抽样）；盘中——事件流无重复、仅增量渲染，退潮/高低切归类在构造样本上符合预期；温度计——与人工「冰点-沸点」判断一致率 ≥80%（抽样 20 日）；仓位——`kellyPosition` 对「p=0.5,b=1」返回 0，对边界参数不越护栏。

## 9. 变更记录

- v0.6（2026-09-14 作战板块重做）：**作战页的产物由"用户自己点选"换成"模型给作战思路"**（主攻方向 + 候选票含触发/失败条件 + 三问落点 + 持仓动作 + 竞价判定），用户只做采纳 / 修改 / 驳回。素材**只来自其他板块**（行情 / 自挖板块 / 选股筛选 / 复盘存档 / 竞价 / 持仓 / 自选），并由 host 半的**素材护栏**执行三条硬约束：票白名单、方向名逐字反查、引文可反查（剔除与未落地都回传显示）。模型结论按**只填空位**写进当日记录并标 `from:'ai'`，人工答案一律不覆盖；休市 / 无素材**不自动生成**，同一时段重复打开 **0 次模型调用**。**红线维持不变**：AI 不覆盖用户草稿（这里是"不覆盖人工答案"）；裁剪与降级不静默。详见 `CHANGELOG.md`。
- v0.5（2026-09-12 A6）：新增 **§6e 导航平铺**——「工具」中间层整体下线，旧抽屉里的板块与复盘/作战同级；摘除「自选盘」「个股明细」（左栏即入口）与「看盘」入口（改为不上导航栏的**工作台**，点股票即到）；`ToolHost`/`TOOL_VIEWS`/`ui.tool`/`t` 键与 Esc 的 "tool" 层一并删除，`toolIdOf` 收敛为 `viewIdOf`。
- v0.4（2026-09-12 校订）：新增 **§6d 信息架构 v1.5 决策**——定位明确为「盯盘执行台」并附能力归属表；左栏改为「自选 + 个股（最近看过）」、点行即渲染个股信息（下线涨停/异动分组）；工具合并为单页、外盘弱化；新增**自挖概念 + 命名**特色板块（host 半移植 cluster-namer 口径与护栏）；新增**复盘校准页**；持久化改 **host 侧领域存储 + localStorage 降级**。同时把 §1–§6b、§7–§8 标注为历史，现行文档拆分为 `README.md` / `docs/ARCHITECTURE.md` / `docs/ROADMAP.md` / `CHANGELOG.md`。
- v0.3（2026-09-11 真机验收）：P2a 三个落点（个股决策卡+图上价位线、复盘预期排序、选股候选排序）落地并在真机确认可见；新增 **AI 自愈重试**：`maxTokens` 是 reasoning 与正文的**共享预算**，推理等级高时模型会把预算烧在思考上（实测 60 根日 K 的上下文直接 0 正文失败），故 host 半在「只回思考不回正文」或「上下文超限」时自动裁剪上下文并重试，以 `meta.shrunk` + UI 提示如实告知。**设计红线维持不变：AI 结果不自动覆盖用户草稿；裁剪不静默。**
