# 用户使用流程（USER-GUIDE）

> 依据 `PRODUCT-DESIGN.md`（§3 流程总览）、`README.md` 与 **2026-09-06 真机 GUI 验收**
> 整理的「用户正确使用流程」：先说明前提与页面结构，再按一天的时间轴给出标准用法，
> 最后给出空数据/休市等边界场景下的正确行为与建议。
>
> 与蓝图（PRODUCT-DESIGN §3.1）的一处落地差异：**复盘模式不是「作战」页内的模式开关**，
> 而是独立 Tab（`作战` + `复盘` 两个 Tab），`作战`页头部按时段自动显示 休市/竞价/盘中 标签，
> 复盘请直接切「复盘」Tab（15:10 后与盘前任意时间均可用）。

---

## 0. 前提

1. DSH Web 已安装本插件（`dsh plugin --profile web add @lisonevf/dsh-stock-panel`）并
   重启 `dsh web`——工作台以**最右侧第四列**出现：`sidebar | 对话区 | details | 📈 工作台`。
2. A 股行情通过 MCP 数据源获取（默认 `192.168.31.196:8007/mcp`，opentdx 3.4.0）。
   数据源离线/休市不影响面板出现，页面会显示对应空态提示（见 §5）。
3. 自选、复盘存档、事件流历史、持仓与交易日志均为 **localStorage 本地存储**，刷新不丢。

## 1. 界面结构：8 个 Tab

```
市场 | 梯队 | 指数 | 自选 | 个股 | 作战 | 复盘 | 监控
```

| Tab | 内容 | 数据 | 刷新 |
| --- | --- | --- | --- |
| 市场 | 指数条 + 广度（涨/跌/涨停/跌停/成交）+ 涨跌分布 + 涨幅/跌幅/成交/换手榜单 + 市场异动 | 全 A 快照 + quote + unusual×3 | 20s |
| 梯队 | 涨停/跌停速览 + 连板梯队条形 + 按板数分组 + 板块热度 TOP12 | 全 A + kline(20)×N + belong_board | 30s |
| 指数 | 9 大指数切换 + 摘要 + 日 K（MA5/10/20+量）/ 分时双模式 | quote + kline + tick_chart | 15s |
| 自选 | 本地自选池 + 12s 实时价 + 搜索/代码添加 | watchlist-store + quote | 12s |
| 个股 | 由市场/梯队/自选/作战点选打开：信息条 + 图表卡（日K 区间/MA 或 分时）+ 竞价回顾 + 资金面板 + 逐笔(折叠) + AI 分析 | kline + tick_chart + auction + capital_flow + transaction + AI(后端) | 手动 |
| 作战 | 时段标签（休市/竞价/盘中）+ 温度计 + 局势 + 板块脉冲 + 事件流 + 竞价雷达 + 持仓决策台 | server_info + unusual/market_monitor + ladder + auction | 30s |
| 复盘 | 今日盘眼 + 自动回顾 + 预期清单编辑器(≤5) + 存档（按日覆盖） | ladder + 全 A + 事件历史 | 手动 |
| 监控 | 规则（价格突破/跌破、涨跌幅、异动关键词）+ 命中记录；命中 → Tab 红点 + 底部 Toast | 规则标的 quote(≤10) + 事件流增量 | 12s |

任意股票行/事件/板块代表 → 点击即跳到「个股」Tab 打开详情；个股页 ‹ 返回回到来源 Tab。
个股页 ⭐ 加/移自选，与「自选」Tab 即时同步。

## 2. 标准用法（按交易日时间轴）

一天的完整闭环对应方法论：**盘后复盘 → 次日竞价对照 → 盘中作战 → 收盘复盘**。

### 2.1 收盘后 / 盘前（任意时间，含周末）→ 「复盘」Tab
1. 打开「复盘」：自动装配 今日盘眼（温度/广度/指数）、涨停梯队、炸板/跌停清单、主线建议。
2. 在 **预期清单（≤5 张）** 点「从涨停池加入」选龙头；或用卡片编辑
   `状态 / 题材天 / 层级 / 角色 / 剧本 / 竞价条件 / 失败条件 / 理由`。
3. 「存档今日复盘」：写入当日快照（按日覆盖，保留 60 个交易日），**刷新不丢**，
   次日竞价/复盘可读取。
4. 周末/休市复盘同样成立：即使当日无行情，也可用本地历史做功课。

### 2.2 次日 9:15–9:25 竞价 → 「作战」Tab
1. 「作战」页头自动显示 **竞价** 标签；竞价雷达挂载（观察池 = 昨日预期 + 自选 + 涨停候选）。
2. 看 竞价雷达列表：每行给出 与昨日预期的对照判定（超预期/证伪/弱转强/陷阱等）——
   **只突出与昨日预期不一致者，一致的整行置灰（降噪）**。
3. 对照自己写的 竞价条件/失败条件 决定开盘动作；9:25 后雷达收起。

### 2.3 盘中 9:30–15:00 → 「作战」Tab（主战场）
1. 顶部一行：`温度计数值 + 档位（总仓闸门）`、`局势标签（Q2）`。
2. **异动事件流**：只显示增量（涨停/跌停/炸板/拉升/大单），收盘后转只读并提示。
3. **板块脉冲**：热度板块的涨停数/涨幅增量，识别资金攻击方向。
4. **持仓决策台**：登记持仓与计划 → 决策条提示 Q1 持续性 / Q2 局势 / Q3 动作；
   平仓记录进交易日志 → 复盘页形成 p̂/b̂ 自统计，样本 ≥50 前凯利建议为空仓（无证据）。
5. 点事件行可跳个股页核价量、竞价、资金流，再回到作战。

### 2.4 收盘 15:10 后 → 回到「复盘」Tab 存档
按 2.1 完成当日复盘 → 形成「明日预期」，闭环到次日的 2.2。

### 2.5 随时 → 市场 / 梯队 / 指数 / 自选
- **市场**：广度与四个榜单定位市场强弱、找当日异动；指数条点芯片跳「指数」页看走势。
- **梯队**：空间高度（最高连板/梯队结构）+ 宽度（板块热度 TOP），识别主线。
- **指数**：切换 9 大指数，日 K 看结构、分时看日内节奏。
- **自选**：把关注标的维护成观察池（行情页 ⭐ 或搜索框加）；涨跌幅排序；
  竞价时段它是竞价雷达观察池的一部分。

## 3. 维护自选（含行情源不可用的场景）

1. 「自选」搜索框输入 **6 位代码或名称** 直接添加（名称命中需要全 A 索引可用）。
2. 行情源暂无 A 股数据（休市/断连）时，搜索名称无命中属正常：
   **直接输入代码**（如 `600519`、`000001`、`SZ000001`）会出现
   「直接加入 SH600519（按代码规则推断市场）」入口，可离线维护观察池，行情恢复后自动出价。
3. 自选上限 60；行内 hover 出现 ✕ 移出，顶部「清空」一键清。

## 4. 个股详情如何打开

三种入口：
- 市场/梯队/自选页点任意股票行；
- 作战页事件流/竞价雷达行；
- 复盘页事件清单 / 主线代表。

个股页自上而下：返回 + 搜索/星标 → 信息条（可自定义列）→ 图表卡
（**日K**：近3月/6月/1年/全部 区间 + MA 开关；**分时**：当日/最近交易日，昨收虚线）→
竞价回顾（当日）+ 资金面板（当日/5日）+ 逐笔成交（折叠）→ AI 四维分析。点标的搜索也可直接换股。

## 5. 边界场景与提示（2026-09-06 验收后行为）

| 场景 | 页面表现 | 建议 |
| --- | --- | --- |
| 非交易时段（周末/节假日/盘前盘后） | 市场页琥珀条「行情源暂无 A 股数据」（20s 自动重试）；指数页「行情源暂无指数数据」；自选行显示「暂无报价（休市/未连接）」；作战页头部显示 💤休市 并停止轮询 | 做「复盘」功课、维护自选、看事件流历史 |
| 数据源完全不可达（MCP 超时） | 页面红条「行情源不可达…」；作战页「温度计暂无数据」 | 检查 `192.168.31.196:8007` 连通性（`MCP-SETUP.md` 排障） |
| 数据源会话偶发失效 | 客户端自动重建会话重试（无需刷新） | — |
| 某标的单只无数据 | 个股页红条「暂无该标的 K 线数据」 | 可能是退市/停牌/代码错误，换标的 |

> 无行情时页面不会再把“空数据”误标成“加载中”，也不会无限转圈——
> 加载中只出现在请求真正进行中（≤15s 超时兜底）。
